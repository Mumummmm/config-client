spring:
  cloud:
    config:
      profile: pro
      label: master
      name: config-client
      discovery:
        enabled: true
        serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/